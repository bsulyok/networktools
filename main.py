import numpy as np
import random
import classes
import models
import drawing
import utils
import networkx as nx
import embedding

def main():
    #G = models.erdos_renyi_graph(20,0.5)
    #G = models.regular_tree(degree=3, max_depth=8)
    # coords = np.random.rand(20)
    #coords = embedding_tools.greedy_embedding(G.adj)
    #drawing.draw(G.adj, coords)
    #GR = utils.greedy_routing_score(G.adj, coords, utils.poincare_hyperbolic_distance)
    #print(GR)
    return

if __name__ == "__main__":
    main()